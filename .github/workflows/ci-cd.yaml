name: Deploy - GKE

on:
  push:
    branches:
      - main
    paths:
      - "k8s/**"
      - ".github/workflows/**"

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v0.2.0
        with:
          project_id: ${{ secrets.GKE_PROJECT }}
          service_account_key: ${{ secrets.GKE_SA_KEY }}
          export_default_credentials: true

      - name: GKE credentials
        run: |-
          gcloud container clusters get-credentials your-cluster-name --zone your-cluster-zone --project ${{ secrets.GKE_PROJECT }}

      - name: Deploy
        run: |-
          kubectl apply -f k8s/
