name: Tech Challenge - Deploy Infrastructure - GKE

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: terraform  # Define o diretório de trabalho padrão para todos os passos

    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v0.2.0
        with:
          project_id: ${{ secrets.PROJECT_ID }}
          service_account_key: ${{ secrets.SA_KEY }}
          export_default_credentials: true

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v1
        with:
          terraform_version: "1.1.0"  # Substitua pela versão desejada do Terraform

      - name: Terraform Init and Apply
        run: |
          terraform init
          terraform apply -auto-approve

      - name: Install gke-gcloud-auth-plugin
        run: gcloud components install gke-gcloud-auth-plugin
        working-directory: .  # Retorna para o diretório raiz para este passo

      - name: GKE credentials
        run: |
          gcloud container clusters get-credentials TechChallenge --zone us-central1 --project ${{ secrets.PROJECT_ID }}
        working-directory: .  # Retorna para o diretório raiz para este passo

      - name: Deploy Kubernetes Configurations
        run: |
          kubectl apply -f k8s/
        working-directory: .  # Retorna para o diretório raiz para este passo
